# No environment variables are required during the foundation stage.
#
# Stage 09 reads these values at build time for the protected contact feature.
# Keep real addresses only in ignored .env.local files and CI/Cloudflare secrets.
# Never rename them with a PUBLIC_ prefix.
SITE_EMAIL_BROWN=
SITE_EMAIL_PERSONAL=
